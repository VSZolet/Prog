#include <stdio.h>
#include <string.h>



int main()
{
        char codigo[], traduzido[]; sequencia[];
        int contador = 0;
        char *p = traduzido;

        while ( (p=strstr(traduzido,sequencia)) != NULL ) {
                p += strlen(sequencia);
                contator++;
        }
        printf("%d ocorrencias de %s\n", total, needle);

}
